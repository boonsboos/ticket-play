@page "/manager/create-movie"

@using ConnectPlay.TicketPlay.Models
@using ConnectPlay.TicketPlay.UI.App.Components.Base

@rendermode InteractiveServer

@inherits TranslatableComponent;

<PageTitle>@T["createMovie.pageTitle"]</PageTitle>

@if (!string.IsNullOrWhiteSpace(successMessage))
{
    <div class="toast-container position-fixed top-0 end-0 p-3">
        <div class="toast show align-items-center text-bg-success border-0" role="alert" aria-live="assertive"
             aria-atomic="true">
            <div class="d-flex">
                <div class="toast-body">
                    @successMessage
                </div>
                <button type="button" class="btn-close btn-close-white me-2 m-auto" aria-label="Close"
                        @onclick="() => successMessage = null"></button>
            </div>
        </div>
    </div>
}

@if (!string.IsNullOrWhiteSpace(errorMessage))
{
    <div class="alert alert-danger">@errorMessage</div>
}

<div class="container my-4 col-8">
    <h3 class="mb-4">@T["createMovie.title"]</h3>
    <div class="card shadow-sm">
        <div class="card-body">
            <EditForm Model="model" OnValidSubmit="HandleValidSubmit" FormName="CreateMovieForm">
                <DataAnnotationsValidator />
                <ValidationSummary />

                <div class="mb-3">
                    <label class="form-label">@T["createMovie.movieTitle"]</label>
                    <InputText class="form-control" @bind-Value="model.Title" />
                    <ValidationMessage For="@(() => model.Title)" />
                </div>

                <div class="mb-3">
                    <label class="form-label">@T["createMovie.description"]</label>
                    <InputTextArea class="form-control" @bind-Value="model.Description" />
                    <ValidationMessage For="@(() => model.Description)" />
                </div>

                <div class="row">
                    <div class="mb-3 col-md-6">
                        <label class="form-label">@T["createMovie.duration"]</label>
                        <InputNumber class="form-control" @bind-Value="model.Duration" />
                        <ValidationMessage For="@(() => model.Duration)" />
                    </div>

                    <div class="mb-3 col-md-6">
                        <label class="form-label">@T["createMovie.releaseDate"]</label>
                        <InputDate class="form-control" @bind-Value="model.ReleaseDate" />
                        <ValidationMessage For="@(() => model.ReleaseDate)" />
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label">@T["createMovie.posterUrl"]</label>
                    <InputText class="form-control" @bind-Value="model.PosterUrl" />
                    <ValidationMessage For="@(() => model.PosterUrl)" />
                </div>

                <div class="row">
                    <div class="mb-3 col-md-4">
                        <label class="form-label">@T["createMovie.language"]</label>
                        <InputSelect class="form-control" @bind-Value="model.Language">
                            <option value="">@T["createMovie.selectLanguage"]</option>
                            <option value="Nederlands">@T["createMovie.languageDutch"]</option>
                            <option value="English">@T["createMovie.languageEnglish"]</option>
                        </InputSelect>
                        <ValidationMessage For="@(() => model.Language)" />
                    </div>

                    <div class="mb-3 col-md-4">
                        <label class="form-label">@T["createMovie.minimumAge"]</label>
                        <InputSelect class="form-control" @bind-Value="model.MinimumAge">
                            <option value="">@T["createMovie.selectMinimumAge"]</option>
                            <option value="@MinimumAgeRating.AL">AL</option>
                            <option value="@MinimumAgeRating.Age6">6</option>
                            <option value="@MinimumAgeRating.Age9">9</option>
                            <option value="@MinimumAgeRating.Age12">12</option>
                            <option value="@MinimumAgeRating.Age14">14</option>
                            <option value="@MinimumAgeRating.Age16">16</option>
                            <option value="@MinimumAgeRating.Age18">18</option>
                        </InputSelect>
                        <ValidationMessage For="@(() => model.MinimumAge)" />
                    </div>

                    <div class="mb-3 col-md-4">
                        <label class="form-label">@T["createMovie.genre"]</label>
                        <InputText class="form-control" @bind-Value="model.Genre" />
                        <ValidationMessage For="@(() => model.Genre)" />
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label">@T["createMovie.tags"]</label>
                    <InputText class="form-control" @bind-Value="model.TagsCsv" />
                    <ValidationMessage For="@(() => model.TagsCsv)" />
                </div>

                <button class="btn btn-primary" type="submit" disabled="@isSubmitting">
                    @(isSubmitting ? T["createMovie.saving"] : T["createMovie.submit"])
                </button>
            </EditForm>
        </div>
    </div>
</div>
